# INTERNAL — triggered on push to main, via merge-gate (workflow_call), and manually; never on pull_request directly.
name: Security

on:
  push:
    branches: [ main ]
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  security:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-security-reports.yml@v3.0.0
    with:
      java-version: '25'
    secrets: inherit
